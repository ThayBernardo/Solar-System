import React from 'react';
import Header from './Header';

class SolarSystem extends React.Component {
  render() {
    return (
      <div data-testid="solar-system">{ Header }</div>
    );
  }
}

export default SolarSystem;
